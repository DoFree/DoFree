/*#include <SFML/Graphics.hpp>



class mapEngine{
public:
	mapEngine(sf::Vector2f taille, sf::Image imgMngr);
	
	
	void DrawMap(sf::RenderWindow Rendu);
	bool LoadMap(à compléter);

private:
	int MapNum[256][256];
	sf::Sprite TamponTiles;
*/

